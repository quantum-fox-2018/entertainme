type Movie {
  title: String
  poster_path: String
  overview: String
  popularity: Int
  status: String
}

type Query {
  movies: [Movie]
  movie(_id: String): Movie
}

type Mutation {
  newMovie(title: String, overview: String): Movie
}
